import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  firstText: {
    alignItems: 'center',
    color: '#808080',
    marginTop: 20,
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 16,
    width: 300,
  },
  secondText: {
    alignItems: 'center',
    color: '#808080',
    textAlign: 'center',
    fontSize: 16,
    width: 300,
  },
})